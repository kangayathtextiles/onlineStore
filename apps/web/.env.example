# KANGAYATH WEB — Frontend Environment Configuration (.env.example)
# Public variables (exposed to browser bundle, MUST be prefixed with NEXT_PUBLIC_)
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME="Kangayath Web"

# Server-only variables (never exposed to browser)
# INTERNAL_API_SECRET=
