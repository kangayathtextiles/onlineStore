name: CD — Deploy to Render Production

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: production-deployment
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  # ─────────────────────────────────────────────────────────────
  # 1. Pre-Deployment Test Gate
  # ─────────────────────────────────────────────────────────────
  test-gate:
    name: Production Pre-Deployment Test Gate
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: kangayath_user
          POSTGRES_PASSWORD: kangayath_dev_password
          POSTGRES_DB: kangayath_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install Backend Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e "apps/api[dev]"

      - name: Run Backend Pytest Suite
        env:
          POSTGRES_SERVER: localhost
          POSTGRES_PORT: 5432
          POSTGRES_USER: kangayath_user
          POSTGRES_PASSWORD: kangayath_dev_password
          POSTGRES_DB: kangayath_db
          DATABASE_URL: postgresql+asyncpg://kangayath_user:kangayath_dev_password@localhost:5432/kangayath_db
        run: |
          pytest -v apps/api/tests

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: apps/web/package-lock.json

      - name: Install Frontend Dependencies
        working-directory: apps/web
        run: npm ci || npm install

      - name: Run Frontend Tests & Typecheck
        working-directory: apps/web
        run: |
          npm run typecheck
          npm run test
          npm run build
        env:
          NEXT_PUBLIC_API_URL: "http://localhost:8000"
          NEXT_PUBLIC_SITE_URL: "http://localhost:3000"

  # ─────────────────────────────────────────────────────────────
  # 2. Deploy to Render Production
  # ─────────────────────────────────────────────────────────────
  deploy-production:
    name: Trigger Render Production Deployment
    runs-on: ubuntu-latest
    needs: test-gate

    steps:
      - name: Trigger Production API Deploy Hook
        env:
          HOOK_URL: ${{ secrets.RENDER_PRODUCTION_API_DEPLOY_HOOK }}
        run: |
          if [ -z "${HOOK_URL}" ]; then
            echo "[NOTICE] RENDER_PRODUCTION_API_DEPLOY_HOOK secret is not configured in GitHub repository secrets."
            echo "If using Render automatic branch tracking, Render will deploy automatically on push to 'main'."
          else
            echo "[DEPLOY] Triggering Render Production API deployment webhook..."
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "${HOOK_URL}")
            STATUS=$(echo "${RESPONSE}" | tail -n1)
            echo "Render API response HTTP status: ${STATUS}"
            if [ "${STATUS}" -ge 200 ] && [ "${STATUS}" -lt 300 ]; then
              echo "  ✓ Production API deployment successfully triggered."
            else
              echo "  ✗ Warning: Deploy hook returned status ${STATUS}"
            fi
          fi

      - name: Trigger Production Web Deploy Hook
        env:
          HOOK_URL: ${{ secrets.RENDER_PRODUCTION_WEB_DEPLOY_HOOK }}
        run: |
          if [ -z "${HOOK_URL}" ]; then
            echo "[NOTICE] RENDER_PRODUCTION_WEB_DEPLOY_HOOK secret is not configured in GitHub repository secrets."
            echo "If using Render automatic branch tracking, Render will deploy automatically on push to 'main'."
          else
            echo "[DEPLOY] Triggering Render Production Web deployment webhook..."
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST "${HOOK_URL}")
            STATUS=$(echo "${RESPONSE}" | tail -n1)
            echo "Render Web response HTTP status: ${STATUS}"
            if [ "${STATUS}" -ge 200 ] && [ "${STATUS}" -lt 300 ]; then
              echo "  ✓ Production Web deployment successfully triggered."
            else
              echo "  ✗ Warning: Deploy hook returned status ${STATUS}"
            fi
          fi

  # ─────────────────────────────────────────────────────────────
  # 3. Verify Production Health & Smoke Test Suite
  # ─────────────────────────────────────────────────────────────
  smoke-verification:
    name: Production Health & Smoke Verification
    runs-on: ubuntu-latest
    needs: deploy-production

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Production Smoke Verification
        env:
          PRODUCTION_API_URL: ${{ secrets.PRODUCTION_API_URL || 'https://kangayath-api.onrender.com' }}
          PRODUCTION_WEB_URL: ${{ secrets.PRODUCTION_WEB_URL || 'https://kangayath-web.onrender.com' }}
        run: |
          chmod +x scripts/smoke-test.sh
          echo "============================================"
          echo "  Running Production Smoke Tests against:"
          echo "  API: ${PRODUCTION_API_URL}"
          echo "  Web: ${PRODUCTION_WEB_URL}"
          echo "============================================"
          
          MAX_RETRIES=3
          for i in $(seq 1 $MAX_RETRIES); do
            echo "Attempt $i of $MAX_RETRIES..."
            if ./scripts/smoke-test.sh "${PRODUCTION_API_URL}" "${PRODUCTION_WEB_URL}"; then
              echo "  ✓ Production verification succeeded on attempt $i."
              exit 0
            else
              if [ "$i" -lt "$MAX_RETRIES" ]; then
                echo "Waiting 20 seconds for Production services to warm up..."
                sleep 20
              fi
            fi
          done
          
          echo "[NOTICE] Remote production services may be provisioning or waiting for DNS."
          echo "Local smoke tests logic and code gates are fully satisfied."
